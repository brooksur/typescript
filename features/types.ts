// Date

const today = new Date();
const year = today.getFullYear();
const month = today.getMonth();
const day = today.getDate();

// Person

const person = {
  age: 20,
};

// person.name -> error

// Class

class Color {}

const red = new Color();
